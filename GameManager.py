from BoardManager import BoardManager, Cell
from Player import Player
class GameManager:
    def __init__(self, boardManager: BoardManager, player: Player):
        self.boardManager = board
        self.player = player 
    def maskBoard(self, board: List[List[Cell]])
    def play(self):
        while boardManager.cells_unvisited:
            realBoard = boardManager.getBoard()
            maskedBoard = 
            move = player.makeMove()